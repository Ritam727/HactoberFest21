#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin >> n;
    int row = 1;
    while (row<=(2*n)+1)
    {
        int col = 2*row - 1;
        while (col--)
        {
            
        }
        
    }
    
    return 0;
}